#!@@@AWK@@@ -f 
# $Id: dshbak.src,v 1.3 2001/11/15 07:41:12 garbled Exp $
# dshbak  *must have nawk or compatible*
BEGIN { LASTNODE="null" }
{
	if ($1 != LASTNODE) {
		LASTNODE = $1
		FOO = $1
		gsub(/./,"-",FOO)
		printf("-----%s\nNode %s\n-----%s\n", FOO, $1, FOO)
	}
	sub(/[^:]*: /,"")
	print $0
}
	
